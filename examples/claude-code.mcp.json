{
  "//": "Claude Code: project-scoped .mcp.json, or register globally with `claude mcp add`.",
  "mcpServers": {
    "context-hub": {
      "command": "npx",
      "args": ["-y", "ai-agent-context-hub"],
      "env": {
        "EMBEDDING_PROVIDER": "local",
        "HUB_DB_PATH": "./data/hub.db"
      }
    }
  }
}
